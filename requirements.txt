# Питон 3.10–3.12. На 3.13+ нет колёс ctranslate2/onnxruntime — setup.sh проверяет это заранее.

# Discord + приём голоса.
#
# Обе версии закреплены точно, и это не перестраховка.
#
# discord.py: с 2.7 в ней есть DAVE (E2EE), и это обязательное условие —
# с 2 марта 2026 голосовой шлюз отвечает close code 4017 «DAVE protocol
# required» всем, кто его не умеет. Весь MLS-обмен (ключевые пакеты,
# welcome, commit, транзишены) библиотека ведёт сама, готовая сессия лежит
# в vc._connection.dave_session. Пин точный, потому что наши заплатки на
# приём зовут её напрямую.
#
# extra [voice] тянет PyNaCl и davey — без davey discord.py вообще
# отказывается создавать VoiceClient.
discord.py[voice]==2.7.1

# Приём голоса: в самой discord.py его нет, вырезан. Это расширение — то же
# самое, что py-cord позже перенесла к себе в 2.8, только без её ошибок
# переноса. Версия alpha и не обновлялась с июня 2025: апстрим не знает про
# DAVE, поэтому расшифровку входящих кадров ставим сами в
# bashmak/audio/voice_recv_patch.py. Обновлять — только проверив живым
# голосом: заплатки завязаны на внутренности PacketDecoder.
#
# ВАЖНО: py-cord и discord.py владеют одним и тем же пакетом `discord` и в
# одном venv несовместимы. setup.sh снимает py-cord перед установкой.
discord-ext-voice-recv==0.5.2a179

# STT
faster-whisper>=1.1

# VAD — silero_vad.onnx крутится напрямую в onnxruntime.
# Пакет silero-vad с PyPI намеренно НЕ ставим: он тянет torch+torchaudio (~800 МБ),
# а нам нужен только сам .onnx (2 МБ) — его качает setup.sh.
onnxruntime>=1.17

# TTS (piper1-gpl, преемник rhasspy/piper)
piper-tts>=1.3

# аудио
numpy>=1.26,<3
soxr>=0.5

# музыка — намеренно без верхнего пина: YouTube ломает старые версии,
# yt-dlp должен обновляться свободно
yt-dlp

# прочее
rapidfuzz>=3.9
httpx>=0.27
PyYAML>=6.0
python-dotenv>=1.0
huggingface_hub>=0.30

# тесты
pytest>=8.0
