# Питон 3.10–3.12. На 3.13+ нет колёс ctranslate2/onnxruntime — setup.sh проверяет это заранее.

# Discord + приём голоса (py-cord, не discord.py — там receive вырезан).
#
# Версия закреплена точно, и это не перестраховка.
#
# Ниже 2.8 идти нельзя: 2.6.x и 2.7.x говорят с голосовым шлюзом по
# протоколу, который Discord больше не принимает — подключение к каналу
# рвётся с кодом 4017 ещё до приёма, и правкой нашего кода это не лечится.
#
# В самой 2.8.1 приёмный стек переписан и при этом недоделан: он требует от
# синка __sink_listeners__, walk_children() и is_opus(), которых нет даже у
# штатных Sink и WaveSink, а start_recording() перестал звать Sink.init(vc),
# без которого декодер пакетов падает на assert. Обходим это в
# bashmak/audio/sink.py и bashmak/bot.py — звук по-прежнему приходит в
# write(), так что хватает объявить недостающее.
#
# Отдельно: экстра [voice] тянет davey, из-за чего библиотека объявляет
# Discord'у поддержку DAVE (E2EE голоса), а принимать его не умеет — путает
# порядок opus_decode и dave.decrypt. Поэтому bashmak/bot.py:disable_dave()
# на старте объявляет версию DAVE нулевой. Удалять davey из зависимостей
# не нужно — правка от этого не зависит.
#
# Диапазон вида >=2.6,<3 приводил к тому, что на новой машине ставилось
# что попало. Обновлять — только осознанно, проверив оба конца: вход в
# голосовой канал и start_recording.
py-cord[voice]==2.8.1
PyNaCl>=1.5

# STT
faster-whisper>=1.1

# VAD — silero_vad.onnx крутится напрямую в onnxruntime.
# Пакет silero-vad с PyPI намеренно НЕ ставим: он тянет torch+torchaudio (~800 МБ),
# а нам нужен только сам .onnx (2 МБ) — его качает setup.sh.
onnxruntime>=1.17

# TTS (piper1-gpl, преемник rhasspy/piper)
piper-tts>=1.3

# аудио
numpy>=1.26,<3
soxr>=0.5

# музыка — намеренно без верхнего пина: YouTube ломает старые версии,
# yt-dlp должен обновляться свободно
yt-dlp

# прочее
rapidfuzz>=3.9
httpx>=0.27
PyYAML>=6.0
python-dotenv>=1.0
huggingface_hub>=0.30

# тесты
pytest>=8.0
